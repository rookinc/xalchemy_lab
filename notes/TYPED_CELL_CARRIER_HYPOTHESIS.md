# Typed Cell Carrier Hypothesis

## Observation

The current sketch appears to define a graded local carrier rather than a uniform tiling.

The visible labels suggest the following cell classes:

- `2` = kernel axis cells
- `3` = reconciliation / interface cells
- `4` = transport cells
- `5` = terminal / boundary cap cells
- `7` = lifted aggregate region

## Interpretation

The carrier appears to be typed by local role.

This means propagation grammar may depend not only on orientation state, but also on cell type.

## Strongest sentence

The sketch suggests a typed carrier with distinct kernel, interface, transport, terminal, and lifted aggregate regions.

