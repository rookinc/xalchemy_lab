# G900 Half-Layer Argument v0.1

## Goal
State the strongest current argument for the final numerical step:

> predecessor-shell inherited displacement equals the half-layer value L/2

This note does not yet claim a closed proof.
It records the cleanest present argument shape.

## Current locked facts

### 1. Carrier-derived layer count
The carrier audit confirms:

- total layer count L = 10

So L is structural.

### 2. Midpoint regime
The midpoint layer is:

- 5

and the center side of the forcing program has isolated:

- I = 145

So the midpoint regime is the carrier of center.

### 3. Predecessor shell
The unique shell immediately before midpoint is:

- layer 4

So the first outer departure from midpoint is uniquely identified.

### 4. Observed centered prism law
The extracted prism law is:

- 140
- 145
- 150

So the observed displacement is:

- d = 5

This equals:

- L / 2 = 10 / 2 = 5

## Main argument

### Principle
If the centered prism law is generated by:

- center from midpoint regime
- scale from first outer departure

then the natural displacement measure is the midpoint half-count of the shell ladder.

In a 10-layer carrier, the midpoint sits at layer 5.
So the first full centered branch displacement is measured relative to the half-layer scale:

- d = 5

### Why this is plausible
The visible prism law is not an arbitrary asymmetric perturbation.
It is the first symmetric one-center / two-branch law extracted from the carrier.

That makes the half-layer count the cleanest structural magnitude available:

- it is carrier-derived
- it is centered on midpoint
- it matches the exact observed displacement
- it does not introduce an extra free parameter

## Candidate lemma
### Half-Layer Displacement Lemma
If branch scale is inherited from the predecessor shell as the first outer departure from a 10-layer midpoint regime, then the inherited displacement is the half-layer value:

- d = L / 2

Hence:

- d = 10 / 2 = 5

and therefore:

- 145 - 5 = 140
- 145 = 145
- 145 + 5 = 150

## What remains open
The only remaining burden is to decide whether this should count as a theorem-grade structural derivation or still as a mechanism proposition.

That is now the final proof-boundary judgment.

