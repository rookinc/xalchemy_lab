# Expanded Actual-Edge Square Family Note

## Checkpoint

After enlarging the partially filled actual-edge lift neighborhood, the supported 4-cycles now split as follows.

### Even 4-cycles
- q0 -> q1 -> q6 -> q3 -> q0
- q0 -> q1 -> q7 -> q4 -> q0
- q0 -> q2 -> q8 -> q3 -> q0
- q0 -> q2 -> q9 -> q4 -> q0

### Odd 4-cycle
- q0 -> q1 -> q5 -> q2 -> q0

## Meaning

The repaired bridge is no longer supported by only one hand-repaired square representative.

There is now a small family of actual even square-type 4-cycles in the enlarged neighborhood.

So the square sector appears to behave like an even cycle class, while the originally chosen odd 4-cycle is excluded from that class.

## Strongest reading

This is evidence that the bridge is stabilizing geometrically:

- return remains even
- twist remains odd
- square appears as a family of even 4-cycles, not a single arbitrary loop

## Next step

Choose one canonical even square representative for the bridge table, while recording that it belongs to a larger even square family.

