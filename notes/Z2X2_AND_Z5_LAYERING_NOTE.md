# Z2 x Z2 and Z5 Layering Note

## Observation

Current local sketch evidence strongly supports a four-state reconciliation layer modeled by:

- `Z2 x Z2`

However, a separate `Z5` structure may still be present at another layer.

## Working hypothesis

The two groups may belong to different parts of the grammar:

- `Z2 x Z2` = local reconciliation / orientation layer
- `Z5` = pentagonal carrier orbit / return layer

## Strongest sentence

`Z2 x Z2` and `Z5` may be different layers of the same grammar rather than competing interpretations.

## Status

- `Z2 x Z2` is currently the stronger local claim
- `Z5` is a plausible outer-cycle or carrier symmetry to test later

