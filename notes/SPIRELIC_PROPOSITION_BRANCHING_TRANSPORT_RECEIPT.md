# Spirelic Proposition: The Local Quotient Law as Receipt of a Branching Transport Geometry

## Proposition

The current local invariant quotient law

\[
R=0,\qquad Q=A+\sigma,\qquad W=A+\tau
\]

is the mod-2 receipt of an underlying branching transport geometry with:

- one primary hinge,
- one persistent odd exchange branch,
- two sibling even continuation sheets.

---

## Geometric content

The local supported 4-cycle scans establish the following hierarchy.

### 1. Primary bifurcation hinge
The edge `e00` is the primary hinge.

It supports exactly three anchored 4-cycle arms:
- one odd arm,
- two even arms.

This split is stable from both orientations of the anchor.

So `e00` is the local bifurcation point.

### 2. Odd exchange branch
The edges `e04` and `e07` lie along a coherent odd-side branch.

Each of them supports:
- two even continuations,
- one odd continuation.

So the odd sector persists along this side as a genuine branch and does not collapse immediately after leaving the anchor.

### 3. Even continuation sheets
The edges `e05` and `e06` lie along two sibling even continuations.

Each of them supports:
- three even continuations,
- zero odd continuations.

So the even sector propagates as a stable continuation sheet.

---

## Algebraic receipt

The local mod-2 receipt of this branching geometry is captured by the invariant triple

\[
(A,\sigma,\tau)\in\mathbb Z_2^3
\]

with:

- \(A\): anchor receipt,
- \(\sigma\): square/even-sheet receipt,
- \(\tau\): twist/odd-branch receipt.

The bridge signature is then the quotient image

\[
(R,Q,W)=(0,\;A+\sigma,\;A+\tau).
\]

So:

- return is fixed by anchor doubling,
- square is the anchor-relative receipt of the even-sheet sector,
- twist is the anchor-relative receipt of the odd-branch sector.

---

## Spirelic reading

The bridge is therefore not most naturally described as a property of isolated loops.

It is better described as:

- a local branching transport field,
- whose downstairs mod-2 receipts are the invariants \(A,\sigma,\tau\),
- whose visible class split is the quotient signature \((R,Q,W)\).

In this reading:

- the even family records in-phase continuation sheets,
- the odd family records exchanged continuation along the odd branch.

---

## Strongest current statement

At the present checkpoint, the local bridge law is best understood as the mod-2 receipt of a branching transport geometry with one exchange branch and two continuation sheets.

That is the current Spirelic form of the result.

---

## Status

This is still a local seeded proposition, not yet the final global theorem.

But it is the first statement that fuses:

- the invariant quotient law,
- the robustness orbit,
- and the observed hinge/branch geometry

into a single local transport picture.

