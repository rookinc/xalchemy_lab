# G900 Predecessor-Shell Scale Inheritance Lemma v0.1

## Goal
State the exact missing lemma on the offset side of the forcing program.

## Current locked facts

### Midpoint regime
The G900 carrier has:

- total layer count L = 10
- midpoint layer = 5

The centered prism center has been isolated at:

- I = 145

So the midpoint regime is the current best carrier of center.

### Predecessor-shell privilege
The predecessor-shell probe confirms:

- layer 4 is the unique shell immediately before midpoint layer 5

So there is a uniquely privileged outer shell adjacent to the midpoint regime.

### Exact observed offset
The extracted centered prism law satisfies:

- top = 140
- mid = 145
- bottom = 150

so the observed displacement is:

- d = 5 = L / 2

## Candidate lemma
### Predecessor-Shell Scale Inheritance Lemma
If the centered prism center is supplied by the midpoint regime, and the only admissible noncentral scale source is the unique predecessor shell immediately before midpoint, then the off-center branch displacement is inherited from predecessor-shell position.

In particular, the visible branch scale is not a free normalization constant.
It is determined by the shell geometry of the carrier.

## Stronger theorem-shaped version
### Candidate stronger form
If:

1. the midpoint regime fixes the scalar center I,
2. the predecessor shell is the unique immediately adjacent outer shell,
3. no other shell is equally privileged to supply branch displacement,

then the prism branch scale must be inherited from the predecessor shell.

So the off-center branches are forced to appear as:

- I - d
- I
- I + d

with d determined by the shell law.

## Exact remaining burden
This lemma still does not by itself prove:

- d = L / 2

It proves only the structural source of scale:

- predecessor shell supplies scale

The final step after this lemma is the half-layer displacement lemma:

- predecessor-shell inherited scale equals L / 2

## Why this matters
Once this lemma is locked, the offset theorem splits cleanly into:

1. predecessor-shell privilege -> scale inheritance
2. shell law -> inherited scale equals 5

That would complete the offset side of the forcing program.

