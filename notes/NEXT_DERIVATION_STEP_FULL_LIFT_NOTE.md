# Next Derivation Step: Full Lift Note

## Current state

The cocycle is now derived from a signed-lift source file.

That is real progress.

But the current source file is still only a partial seed supporting the named bridge specimens.

## What the paper requires

The actual signed-lift layer on G15 should specify lift behavior on all 30 base edges, with two lifted edges upstairs per base edge.

So the next derivation-grade milestone is:

- 15 base vertices
- 30 lift vertices
- 30 base edges
- 60 lifted edges

Only then does the lift source cover the full G15 cocycle layer.

## Immediate task

Run the signed-lift source validator and make the project explicitly report whether the current source is only a partial seed or a full signed-lift realization.

