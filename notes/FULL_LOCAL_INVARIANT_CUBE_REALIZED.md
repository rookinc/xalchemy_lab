# Full Local Invariant Cube Realized

## Checkpoint

A targeted search for the previously missing invariant state
\[
(1,1,0)
\]
succeeded.

It is realized by all tested flips of the form:
- e00
- one edge from {e02,e05,e10}
- one edge from {e01,e04,e07}

So the previously missing state was not geometrically forbidden.
It was only absent from the earlier sampled perturbation orbit.

## Main result

All 8 states of
\[
(A,\sigma,\tau)\in\mathbb Z_2^3
\]
are now realized in the tested local signed-lift model.

Therefore the full local invariant cube is populated.

## Consequence

The bridge signature is genuinely the quotient image of the full invariant state space under
\[
\Phi(A,\sigma,\tau)=(0,\;A+\sigma,\;A+\tau).
\]

So the bridge signature is many-to-one over the invariant cube.

In particular, the theorem signature
\[
(0,0,1)
\]
has at least two invariant preimages:
- (0,0,1)
- (1,1,0)

## Strongest reading

The local bridge has now been compressed to a complete quotient picture:

- edge-level lift data
- full invariant cube \((A,\sigma,\tau)\)
- quotient bridge signature \((R,Q,W)\)

