# Lift + Twist Half-Step

## Clarification

Lift + twist is not best understood as a vertex label.

It is better understood as a half-step transition:

- `0.5 = lift + twist`

## Interpretation

The kernel transition may have internal microstructure:

- `r0` = raw spark
- `r0.5` = lift+twist reconciliation step
- `r1` = fully resolved centroid-oriented initial state

## Strongest sentence

Lift + twist is a half-step reconciliation move inside the kernel.

