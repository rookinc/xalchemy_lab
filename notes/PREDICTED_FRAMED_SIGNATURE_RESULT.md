# Predicted Framed Signature Result

## Result

The framed signature `(H,S)` can now be predicted structurally from:

- controller sequence `(A,sigma,tau)`
- handedness sequence

without requiring post-hoc reconstruction from runtime receipts.

## What is predicted

The predictor computes:

- chart trace
- transition signs
- `H`
- `S`

and matches the native runtime accumulator on all tested loops.

## Strongest conclusion

The framed signature is now not only observable and accumulable, but structurally predictable.

## Strongest sentence

The framed signature `(H,S)` is now a structurally predictable quantity of the controller sequence.

