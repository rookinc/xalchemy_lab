# G900 Offset Uniqueness Path A Result v0.1

## Checkpoint
The direct-derivation probe materially strengthens the half-layer route.

## Established points

### 1. Carrier-derived layer count
The layer audit confirms:
- L = 10

is carrier-derived.

So the candidate quantity L/2 is not decorative metadata.

### 2. Midpoint / predecessor-shell architecture
The predecessor-shell probe confirms:
- midpoint layer = 5
- unique predecessor shell = 4

So the current mechanism
- midpoint supplies center
- predecessor shell supplies first outer branch scale

is structurally meaningful.

### 3. Predecessor-shell privilege
Layer 4 is the unique shell immediately before midpoint.
This makes it the strongest candidate source for first branch-scale inheritance.

### 4. Competing shell-type quantities
The obvious rival shell-derived quantities fail by structural type:

- predecessor index 4 is a shell label, not a branch displacement law
- midpoint-predecessor gap 1 is adjacency, not branch scale
- bit imbalance 3 is local skew, not centered branch displacement
- parity gap 15 belongs to a higher quotient refinement, not the prism layer

### 5. Exact-match formulas are not equally meaningful
Several formulas happen to equal 5, but only

- L / 2

currently has the correct structural interpretation:
- midpoint-centered
- carrier-derived
- compatible with the centered prism branch picture

## Result
Path A now supports the strongest current direct-derivation reading:

- the centered prism offset is not merely a fit,
- it is the uniquely structurally interpretable midpoint-centered branch displacement currently visible in the carrier data,
- namely d = L / 2 = 5.

## Honest boundary
This still stops just short of a fully closed direct theorem.
What is gained is that the half-layer rule is now structurally preferred on direct grounds, not merely rescued by exclusion of visible rivals.

