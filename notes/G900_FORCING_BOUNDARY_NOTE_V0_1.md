# G900 Forcing Boundary Note v0.1

## Checkpoint
The current G900 descent now has a clear proof boundary.

There is a strong theorem stack below the forcing line, but the forcing line itself is still open.

## What is locked

### 1. Observed centered prism law
The extracted weighted prism satisfies:

- top = 140
- rung = 145
- bottom = 150

with exact identities:

- 145 = (140 + 150) / 2
- 140 = 145 - 5
- 150 = 145 + 5
- 290 = 140 + 150
- 290 = 2 * 145

So the centered prism law is an exact observed result.

### 2. Coarse collapse law
The prism-to-triangle collapse yields coarse edge law:

- 140 + 150 = 290

uniformly across the coarse triangle.

### 3. Half-layer offset fit
The current extracted offset satisfies:

- layer count L = 10
- offset d = 5
- d = L / 2

So the visible centered law can be written as:

- (I - d, I, I + d) = (140,145,150)

with:

- I = 145
- d = 5

### 4. Predecessor shell privilege
The predecessor-shell probe shows:

- midpoint layer = 5
- predecessor shell = 4

and layer 4 is the unique shell immediately before midpoint.

So the predecessor shell is positionally privileged relative to the midpoint regime.

### 5. Sector-side simplification
The bridge-side anchored fan now simplifies to:

- anchor
- even-family sector
- unique odd-branch sector

And the two sibling even arms already merge at the current local invariant descent layer.

So the current bridge question is now sectoral, not packet-based.

## What is not yet proved

The following is still open:

> that the observed center value 145 is structurally forced as the unique center,
> and that the off-center displacement 5 is structurally forced as the unique branch offset.

Current status:

- midpoint balance is observed exactly
- predecessor-shell scale is strongly motivated
- half-layer offset fits exactly
- but the forcing statement is not yet derived from first principles

## Honest boundary
Arithmetic alone does not force the midpoint law.

So the current result is:

- observed centered prism law
- not yet forced centered prism law

Likewise, the half-layer law is currently:

- best geometric candidate
- not yet rigorously justified geometric theorem

## Strongest current mechanism reading
The best current mechanism remains:

- center from midpoint regime
- scale from the last full outer shell before midpoint

This is now strongly supported by:
- exact fit,
- unique predecessor-shell position,
- and stable descent behavior.

But it remains a mechanism proposition rather than a completed forcing proof.

## Exact theorem frontier
The sharp next theorem target is:

### Centered Sector Descent Theorem
Under the G900 descent:

- the rigid anchor descends to the centered rung 145,
- the merged even-family sector descends to one off-center branch,
- the unique odd-branch sector descends to the other off-center branch,
- and the off-center displacement is the predecessor-shell half-layer offset 5.

What remains to be proved is that this structure is forced, not merely observed.

## One-line summary
The theorem stack below the forcing line is strong.
The forcing line itself is still open.

