# Orbit Classifier Result

## Main outcome

The local bridge signature is genuinely compressed by the cluster invariants.

The tested perturbation orbit shows:

1. many different edge-level perturbations collapse to the same invariant triple \((A,\sigma,\tau)\);

2. each tested invariant triple determines exactly one bridge signature via
   \[
   (R,Q,W)=(0,\;A+\sigma,\;A+\tau).
   \]

## Important consequence

The bridge signature is not an edge-level accident.
It is a quotient of the invariant state space.

So the present local picture is:

- edge-level lift data
  -> cluster invariant triple \((A,\sigma,\tau)\)
  -> bridge signature \((R,Q,W)\)

## Key theorem bucket

The theorem bucket
\[
(A,\sigma,\tau)=(0,0,1)
\]
contains many realizations, not just the baseline seed.

So the class-based bridge signature
\[
(R,Q,W)=(0,0,1)
\]
is realized by a nontrivial local orbit.

## Strongest current reading

The bridge has now been compressed from:
- local code behavior
to:
- invariant-level state space
to:
- a quotient bridge signature law.

