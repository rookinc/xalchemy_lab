# G900 Scale Inheritance Argument v0.1

## Goal
State the cleanest current argument for the remaining structural step:

> predecessor-shell privilege implies scale inheritance

This note does not yet claim a closed proof.
It records the strongest present argument shape.

## Current locked facts

### 1. Midpoint regime is isolated
The carrier has:

- total layer count L = 10
- midpoint layer = 5

The center side of the forcing program has isolated:

- I = 145

So the midpoint regime is the current carrier of center.

### 2. The predecessor shell is uniquely privileged
The predecessor-shell probe shows:

- layer 4 is the unique shell immediately before midpoint layer 5

No other outer shell has this exact positional role.

So if a noncentral branch scale is inherited from shell structure, layer 4 is the uniquely privileged candidate source.

### 3. The prism law is centered around I
The observed exact centered prism law is:

- 140
- 145
- 150

So the branch values appear as symmetric off-center departures from the midpoint value 145.

This means the remaining question is not where center lives.
It is where branch displacement comes from.

## Main argument

### Principle
If center is assigned to the midpoint regime, then noncentral displacement should come from the nearest structurally distinguished outer regime rather than from an arbitrary remote shell.

### Application
The only shell with that role is the predecessor shell:

- it is outer relative to midpoint,
- it is immediately adjacent to midpoint,
- it is unique,
- and it belongs to the same regular shell ladder.

So the predecessor shell is the only structurally natural candidate to supply the first branch-scale departure away from center.

## Candidate lemma statement
### Predecessor-Shell Scale Inheritance Lemma
If the centered prism center is fixed by the midpoint regime, and the descent admits exactly one immediately adjacent outer shell with privileged position relative to midpoint, then the off-center branch scale is inherited from that predecessor shell.

Equivalently:
the branch displacement is not a free normalization parameter and not a contribution from an arbitrary remote shell.
It is the first shell-scale departure away from the midpoint regime.

## Why this is plausible
This argument is supported by the current carrier geometry:

1. midpoint and predecessor shell are consecutive layers
2. the predecessor shell is unique
3. the prism law shows only a first symmetric departure from center, not a multi-shell or irregular offset pattern
4. the descent currently exposes a one-center / two-branch structure, which is exactly the shape expected from:
   - center from midpoint
   - scale from first outer departure

## What this does and does not prove

### What it supports
It strongly supports the structural claim:

- predecessor shell supplies scale

### What it does not yet prove
By itself, it does not yet prove the numerical identity:

- d = L / 2 = 5

That is a separate half-layer displacement step.

So this note closes only the source-of-scale question, not yet the exact magnitude-of-scale question.

## Strongest current reading
The forcing program now has the following conceptual split:

### Center
- midpoint regime supplies center

### Scale
- predecessor shell supplies first off-center departure

### Magnitude
- half-layer law supplies the exact displacement value

This is currently the cleanest mechanism decomposition of the centered prism law.

## Exact remaining burden
After this argument, the narrowest remaining step is:

> why predecessor-shell inherited displacement is measured exactly by the half-layer count L/2

That is the last numerical lemma.

