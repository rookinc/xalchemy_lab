# G900 Offset Uniqueness Path A Probe v0.1

## Goal
Probe whether the offset

- d = 5

can be derived directly from midpoint-centered shell structure, rather than only by exclusion of rivals.

## Direct question
Among shell-derived quantities attached to the midpoint/predecessor-shell pair, which one has the right structural type to serve as the first prism branch displacement?

## Candidate quantities
The current obvious candidates are:

1. midpoint index = 5
2. predecessor shell index = 4
3. midpoint-predecessor gap = 1
4. half-layer count = L/2 = 5
5. shell population differences
6. shell contact differences
7. shell-local bit imbalance

## Required structural type
A valid direct offset source must be:

- midpoint-centered,
- predecessor-shell attached,
- symmetric as a branch displacement,
- first-order in the shell ladder,
- exact on the observed prism law.

## Probe target
Determine whether only the half-layer quantity has that structural type.

If so, then Path A upgrades from:
- a good fit

to:
- a direct structural derivation candidate.

