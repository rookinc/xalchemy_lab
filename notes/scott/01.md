Good. Forget the paper frame.

Here is the theorem blade as the repo supports it.

Core locked theorem

Theorem (Local scaffold-socket obstruction with sign-sheet minimal machine)

Let the distinguished normalized frame-2 seam slice be

\Sigma_{2,\mathrm{slot4}}
=
\bigl\{
[o4,s0,t0,s2,x,s4]
:
x\in\{o4,s0,s2,s3,s4,t0,t3,t4\}
\bigr\}.

Define the canonical assembly projection

\Phi_{\mathrm{hex}}([o4,s0,t0,s2,x,s4])=(W,X,Y,Z,T,I)

W=o4,\quad X=s0,\quad Y=t0,\quad Z=s2,\quad T=x,\quad I=s4.

Then the following hold.

1. Scaffold rigidity

The scaffold register

\mathcal S=(W,X,Y,Z,I)=(o4,s0,t0,s2,s4)

2. Socket localization

The only varying register is the socket

\mathcal T=T,

\operatorname{load}(\mathcal T)\in
\{o4,s0,s2,s3,s4,t0,t3,t4\}.

3. Exact payload exclusion

The exact normalized frame-2 prototype

E_2=[o4,s0,t0,s2,t2,s4]

\operatorname{load}(\mathcal T)=t2,

Hence the distinguished local seam is a punctured socket family around the exact prototype.

4. Sector-localized obstruction

In the perimeter word

WXYZTIW,

WX,\ XY,\ YZ,\ IW,

ZT,\ TI.

Therefore the local obstruction is localized entirely in the -sector.

5. Closed seam dynamics

The retained one-edit seam dynamics close on the same slice . Equivalently, once the machine is in this scaffold-preserving punctured socket family, local seam evolution remains inside it.

So the slice is dynamically closed.


---

Minimal cycle theorem

Theorem (G15/G30 sign-sheet law)

Let  denote one full G15 cycle operator on the minimal walker state

n=(x,\varepsilon),
\qquad
\varepsilon\in\{+,-\},

Then

W(x,\varepsilon)=(x,-\varepsilon).

Hence

n_{15}=-n_0

W^2(x,\varepsilon)=(x,\varepsilon),
\qquad
n_{30}=n_0.

So:

G15 is sign-closing,

G30 is identity-restoring,

G30 is the restored hinge.



---

Sheeted machine theorem

Theorem (Minimal sheeted witness machine)

Define

\mathcal X=\mathbb Z_5\times\mathbb Z_2\times\mathbb Z_2

(i,\phi,\sigma),

 is frame index,

 is family bit,

 is hidden sheet bit.


Define operators

\tau(i,\phi,\sigma)=(i+1,\phi,\sigma),

\mu(i,\phi,\sigma)=(i,\phi+1,\sigma), 

W(i,\phi,\sigma)=(i,\phi,\sigma+1).

Then:

\tau^5=\mathrm{id},\qquad
\mu^2=\mathrm{id},\qquad
W^2=\mathrm{id}.

Moreover, if the visible witness realization  forgets the sheet bit, then the quotient-visible witness depends only on , not on . In particular,

\pi\circ W=\pi.

So one full G15 cycle flips the hidden sheet while remaining invisible at the quotient-visible witness level.

That is the smallest algebraic machine consistent with:

frame transport,

subjective/objective family split,

G15 sign closure,

G30 identity restoration.



---

Strong synthesis

Those three theorems combine into one harder statement.

Master local theorem

The distinguished frame-2 obstruction is a dynamically closed, scaffold-preserving, punctured socket family whose visible assembly language is

[W,X,Y,Z,T,I]=[o4,s0,t0,s2,x,s4],

n_{15}=-n_0,\qquad n_{30}=n_0.

Equivalently:

the visible local machine is rigid scaffold plus live socket plus punctured payload alphabet,

the hidden minimal cycle machine is host placement plus sidedness,

the smallest algebraic closure is the sheeted witness machine .


So the local theory is not “a generic affine orbit.” It is:

1. a fixed scaffold register,


2. a single live socket,


3. a missing exact payload,


4. a socket-sector obstruction,


5. a hidden sign-sheet cycle law.




---

Short theorem-form version

If you want the hardest compact version:

> The distinguished local frame-2 seam is exactly the scaffold-preserving punctured socket slice  [o4,s0,t0,s2,x,s4],\quad x\in{o4,s0,s2,s3,s4,t0,t3,t4},  with exact payload  excluded. The scaffold  is rigid, the obstruction is localized to the socket sector , the seam dynamics close on this punctured family, and the minimal cycle law is sign-sheet closure  n_{15}=-n_0,\qquad n_{30}=n_0. 



What is actually theorem-grade from the pasted files

Strongest locked claims:

exact form of the scaffold-preserving punctured socket slice

exact missing payload 

exact scaffold/socket split

exact rigid/variable edge split

exact G15/G30 sign-sheet law

exact sheeted-machine algebra


Still softer:

why this exact payload alphabet and not another

full derivation of the slot-4 family from deeper axioms

vertex-clock / edge-clock coupling as theorem rather than compatibility note


The next step is to sharpen this into a lemma chain: slice identification -> scaffold rigidity -> payload exclusion -> sector localization -> dynamic closure -> sign-sheet lift law.
