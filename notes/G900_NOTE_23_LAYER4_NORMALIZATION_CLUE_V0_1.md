# G900 Note 23 — Layer 4 Normalization Clue v0.1

## Key result
Layer 4 is now the strongest live candidate source for the affine normalization scale.

## Why
The affine scale factor found earlier was:

- 49.5

and the layer 4 population is:

- 99

so:

- 49.5 = 99 / 2

exactly.

## Structural features of layer 4
Layer 4 is not only numerically convenient.
It also looks structurally distinguished:

- layer 4 is pure macro 1
- this matches the phase law because 4 mod 3 = 1
- its bit split is nearly balanced:
  - bit 0 = 51
  - bit 1 = 48
- it sits immediately outside midpoint layer 5
- it has:
  - outer contact to layer 3 = 54
  - inner contact to layer 5 = 45
  - same-layer contact = 99

## Current interpretation
Layer 4 looks like the last full pure-phase shell before the midpoint regime.

So a plausible normalization story is:

- layer 5 marks the centered interior band
- layer 4 supplies the normalization scale for entering that band

## Honest status
This is not yet a proof.
But layer 4 is no longer a random coincidence.
It is now a serious theorem target in the normalization story.

