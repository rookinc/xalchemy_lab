# Actual-Edge Bridge Checkpoint R+2 Note

## Checkpoint

After the R+2 expansion, the filled local lift neighborhood supports a complete scan of the currently available 4-cycles.

Totals:
- supported 4-cycles = 18
- even 4-cycles = 13
- odd 4-cycles = 5

## Main result

The bridge is no longer supported only by one repaired square representative.

Instead, the actual-edge geometry exhibits:

- a dominant even 4-cycle family
- a smaller odd 4-cycle family

The chosen `global_square` now sits inside the even family.
The chosen `global_twist` sits inside the odd sector.

## Strongest current reading

The local/global bridge is stabilizing geometrically.

At this checkpoint:

- mismatch parity remains the kernel channel
- local lift_bit remains the active local bridge observable
- return-type remains even
- square-type is best understood as the even 4-cycle family
- twist-type is best understood as the odd sector

## What remains next

The next mathematical move is to extend the lift farther and test whether the even/odd family split persists beyond the current local neighborhood.

