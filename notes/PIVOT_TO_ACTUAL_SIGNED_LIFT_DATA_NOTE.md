# Pivot To Actual Signed-Lift Data Note

## Reason for pivot

The provisional symbolic bridge model has now been stress-tested systematically through word length 5 with no mismatches.

So further short-word brute-force testing is no longer the highest-value next move.

The bottleneck has shifted.

It is now:
- not local transport,
- not symbolic vocabulary,
- but actual signed-lift data.

## New target

Replace the provisional symbolic evaluator with actual signed-lift loop representatives and actual cocycle values.

## First practical step

Create a storage file for actual signed-lift loop artifacts keyed by the current bridge specimen names:

- global_return
- global_square
- global_twist

That file should eventually hold:
- explicit base walk
- actual lift representative
- true cocycle value

## Current state

At the moment, the new artifact file is only a stub.

That is correct.
It marks the precise seam where real signed-lift geometry must enter the project.

