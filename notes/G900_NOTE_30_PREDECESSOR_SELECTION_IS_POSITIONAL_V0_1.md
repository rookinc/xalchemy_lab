# G900 Note 30 — Predecessor Selection Is Positional v0.1

## Result
Layer 4 is not uniquely selected because of an anomalous local shell statistic.

Across the shell family, many layers share the same nice structural properties:
- pure macro phase
- fixed small bit imbalance
- regular contact decay
- strict shell-family regularity

So those features do not isolate layer 4 by themselves.

## What does isolate layer 4
Layer 4 is uniquely selected by position.

For the current G900 carrier:
- layer count L = 10
- midpoint layer = 5
- predecessor shell = 4

Layer 4 is therefore:
- the unique outer shell immediately before midpoint
- still part of the regular outer shell family
- exactly the shell whose half-invariant matches the affine scale inverse

## Refined interpretation
The normalization story is now cleaner:

- midpoint shell supplies center
- predecessor shell supplies scale

So layer 4 matters not because it is weird, but because it is the unique pre-midpoint shell.

## Why this matters
This upgrades the primary hypothesis.

The selection rule is no longer:
- choose a shell that looks numerically special

It is now:
- choose the shell in the structural role immediately preceding midpoint

That is more constrained and more theorem-shaped.

## Honest status
This still does not prove that the export must use predecessor-shell scaling.
But it does sharpen the hypothesis by replacing anomaly language with positional structure.

