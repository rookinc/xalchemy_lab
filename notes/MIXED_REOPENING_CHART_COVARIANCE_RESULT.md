# Mixed Reopening Chart Covariance Result

## Result

The mixed reopening law has two distinct readings:

### Physical-edge reading
- 011 and 111 appear identical in literal edge labels:
  - left -> e_right
  - right -> e_left

### Chart-coordinate reading
After translating exits into chart-relative coordinates:

- 011 becomes:
  - left -> chart_right
  - right -> chart_left

- 111 becomes:
  - left -> chart_left
  - right -> chart_right

So the mixed reopening law is chart-reversed across A.

## Conclusion

The current runtime law is:

- physically identical in literal edge labels
- chart-covariant in local frame coordinates

This means A acts as a genuine local frame involution at the mixed reopened state, but that involution is visible only after translating into chart coordinates.

## Strongest sentence

Mixed reopening is chart-covariant, even when it appears physically identical in raw edge labels.

