# Trurtle xyz Naming

## Proposal

Name the trurtle as the local triadic frame:

- `xyz`

## Interpretation

The trurtle is the coordinated three-channel local carrier entering reconciliation.

In formal language:

- the trurtle is the `xyz` frame acted on by the lift+twist half-step

## Strongest sentence

The trurtle is the local triadic frame `xyz` entering reconciliation.

## Usage

- use **trurtle** when speaking conceptually
- use **xyz** when speaking formally about the local frame


## Orderedness

The trurtle frame `xyz` is ordered.

So permutations such as:

- `xyz`
- `xzy`
- `yzx`

are not assumed equivalent at input.

Order matters before reconciliation.

## Strongest sentence

The trurtle is an ordered triadic frame `xyz`.

