# G900 Opening Axis Lock

Date: 2026-03-29

The opening axis for the first G900 paired-scale experiment is now locked as:

- shell index \(k\)

Not tick.

This means the first canonical experiment reads the lawful output radially rather than chronologically.

## First-pass quantities

- \(N_k\): observed occupancy in shell \(k\)
- \(S_k\): empirical shell envelope at shell \(k\)
- \(C_k\): cumulative observed occupancy through shell \(k\)

## First-pass images

\[
\rho_{\mathrm{shell}}(k)=\frac{N_k}{S_k}
\]

\[
\rho_{\mathrm{bulk}}(k)=\frac{N_k}{C_k}
\]

\[
\Delta(k)=\rho_{\mathrm{shell}}(k)-\rho_{\mathrm{bulk}}(k)
\]

\[
R(k)=\frac{\rho_{\mathrm{shell}}(k)}{\rho_{\mathrm{bulk}}(k)}
\]
when defined.

## Why shell wins first

Shell is the cleaner opening axis because:

- it matches the bridge program’s boundary/bulk intuition
- it aligns with the existing graph-shell geometry language
- it is closer to the Thalean shell-vector rung
- it gives a direct radial readout of lawful output

Tick may still matter later, but shell is the first lens axis.

