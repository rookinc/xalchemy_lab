# Socket Branch Machine Summary

## Status
Cockpit note

## Purpose
Summarize the local frame-2 obstruction in the simplest machine language now available.

---

## 1. Exact machine point

Exact local install:
\[
[W,X,Y,Z,T,I]=[o4,s0,t0,s2,t2,s4].
\]

This is the branch point.

---

## 2. Scaffold

Fixed scaffold register:
\[
\mathcal S=(W,X,Y,Z,I)=(o4,s0,t0,s2,s4).
\]

Live socket:
\[
\mathcal T=T.
\]

---

## 3. Exact payload

Exact payload:
\[
\operatorname{load}(\mathcal T)=t2.
\]

At this payload, the local seam neighborhood opens into all six Hamming-1 directions around the exact prototype.

So the exact state is the local shell junction.

---

## 4. Punctured branch

Punctured socket branch:
\[
[W,X,Y,Z,T,I]=[o4,s0,t0,s2,x,s4]
\]
with payload
\[
\operatorname{load}(\mathcal T)=x\neq t2.
\]

In the bounded theorem program, the observed payload alphabet is
\[
\{o4,s0,s2,s3,s4,t0,t3,t4\}.
\]

At punctured payloads, seam-to-seam local evolution stays on this same socket branch.

So the branch is closed once entered.

---

## 5. Operational readout

Exact point:
- full shell opens.

Punctured point:
- scaffold fixed,
- socket live,
- payload varies,
- local evolution stays on the socket branch,
- exact payload does not reappear locally.

---

## 6. One-line summary

Exact payload is the junction.  
Punctured payloads live on a closed socket branch.

